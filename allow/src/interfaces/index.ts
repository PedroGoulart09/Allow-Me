export interface ContentArticle {
  title: string;
  author: string;
  date: string;
  subtitle: string;
}

export interface Article {
  title: string;
  author: string;
  date: string;
  subtitle: string;
}